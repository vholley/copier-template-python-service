[project]
name = "copier-template-python-service"
version = "0.0.0"
requires-python = ">=3.13"

[dependency-groups]
dev = [
    "copier>=9.0",
    "pyright>=1.1.414",
    "pytest>=8.3",
    "pyyaml>=6.0.3",
    "ruff>=0.16.7",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
