---
description: Bind the current branch to a new work item (after a bounce)
---

Run `make adopt-branch $ARGUMENTS` and report its output verbatim. If it prints a BLOCKED message, relay it and stop.
