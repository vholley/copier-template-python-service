---
description: Leave the process for this branch; then make accept STAGE=opt-out
---

Use the opt-out skill: reply with the canonical opt-out text, then run `make opt-out $ARGUMENTS` to prepare the record. The engineer signs with `make accept STAGE=opt-out`.
