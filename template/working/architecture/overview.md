# Architecture overview

Placeholder written by the adoption step or the Project workflow (`/architect`).
Describe: the workspace members and what each is for, the layer model, the
provider module for cross-cutting concerns, and where configuration enters.
This file has a length budget (`area_doc.max_lines`).
